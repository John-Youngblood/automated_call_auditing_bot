# Pinned to compatible ranges; run `pip freeze > requirements.lock` for deploys.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0        # [standard] pulls uvloop + httptools + websockets
                                    # (the dashboard socket needs the last of these)
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
httpx>=0.27,<1.0
python-multipart>=0.0.9,<1.0       # Twilio posts application/x-www-form-urlencoded

# Persistence for the blocklist and call history. Async driver so a database
# call cannot stall the event loop serving dashboard sockets and webhooks.
sqlalchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20,<1.0
